name: mageos-headless
label: Mage-OS Headless
description: Backend + GraphQL API only — no Luma storefront
selection:
  disabledSets: [luma]
  disabledLayers: []
  enabledAddons: []
  profileGroups:
    theme: luma
    checkout: default
